import './index.css';


function Header({ className }: { className: string }) {
    return (
        <div className={className}>
            Header/index.tsx
        </div>
    );
}

export { Header };
